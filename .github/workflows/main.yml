name: List Root Directory Contents

on:
  push:
    branches:
      - main

jobs:
  list_contents:
    runs-on: ubuntu-latest

    steps:
      - name: List Root Directory Contents
        run: ls -l .
